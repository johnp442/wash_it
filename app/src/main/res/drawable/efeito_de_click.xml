<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape>
            <!-- Defina a cor de fundo quando o botão for pressionado -->
            <solid android:color="@color/btnEfeito" />
        </shape>
    </item>
    <item>
        <shape>
            <!-- Defina a cor de fundo padrão quando o botão não estiver pressionado -->
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
</selector>
